{% extends 'base.html.twig' %}

{% block body %}
    <!-- Content Wrapper. Contains page content -->
    <div
        class="content-wrapper">

        <!-- Main content -->
        <section class="content">
			<br>
            <div
                class="container-fluid">
                <!-- Info boxes -->
                <div class="row">
                    <div class="col-12 col-sm-6 col-md-3"></div>
                    <!-- /.col -->

                </div>
                <!-- /.row -->
                <div class="row">
					<div class="col-md-12">
						<div class="card">
						 <h3 class="card-title">Liste des stocks disponibles <a href="{{path('stock_index')}}"><i class="fa fa-pen"></i></a></h3>
							<div class="card-body table-responsive p-0" style="height: 390px;">
								<table class="table table-head-fixed table-bordered">

									<tbody>
										<tr>
											{# <th>ID</th> #}
											<th>Produit</th>
                                            <th>P.Achat</th>
                                            <th>P.Vente</th>
                                            {# <th>Qd</th> #}
                                            <th>Qt</th>
                                            <th>Qs</th>
                                            <th>T.Achat</th>
                                            <th>T.Vente</th>
                                            {# <th>Prof.Unit</th> #}
                                            <th>Prof.Tot</th>
											
										</tr>
										{% for stock in stocks %}
											<tr>
												{# <td>{{stock.reference}}</td> #}
                                                <td>{{stock.nomFamille}} {{stock.nomProduit}}</td>
                                                <td>{{stock.pau | number_format(0, ' ', ' ')}} FCFA</td>
                                                <td>{{stock.pvu | number_format(0, ' ', ' ')}} FCFA</td>
                                                {# <td>{{stock.qd| number_format(0, ' ', ' ')}}</td> #}
                                                <td>{{stock.qt| number_format(0, ' ', ' ')}}</td>
                                                <td>{{stock.qs| number_format(0, ' ', ' ')}}</td>
                                                <td>{{stock.pat | number_format(0, ' ', ' ')}} FCFA</td>
                                                <td>{{stock.pvt | number_format(0, ' ', ' ')}} FCFA</td>
                                                {# <td>{{stock.bvu | number_format(0, ' ', ' ')}} FCFA</td> #}
                                                <td>{{stock.bvt | number_format(0, ' ', ' ')}} FCFA</td>
                                            </tr>

										{% else %}
											<tr>
												<td colspan="2">Liste vide !</td>
											</tr>
										{% endfor %}

									</tbody>
								</table>

							</div>
							<!-- /.card-body -->
						</div>
					</div>

                </div>
					
                <!-- /.row -->
            </div>

        </section>
    </div>
{% endblock %}
